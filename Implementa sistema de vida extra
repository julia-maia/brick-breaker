int vidas = 3;

void perderVida() {
  setState(() {
    vidas--;
    if (vidas <= 0) {
      // fim de jogo
      mostrarGameOver();
    } else {
      // reinicia posição da bola
      resetarBola();
    }
  });
}

void ganharVidaExtra() {
  setState(() {
    vidas++;
  });
}
